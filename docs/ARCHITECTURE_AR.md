# معماريّة v0.2

OpenPyXL للتحرير المنظم، Pandas/Polars للتحليل، FastExcel/Calamine للقراءة السريعة، XlsxWriter للإنشاء الجديد، ZIP/XML لـSafeEdit، oletools لـVBA، وxlwings/pywin32 كجسر اختياري إلى Excel.

SafeEdit: Backup → Inspect → تحديد worksheet XML → تعديل الخلايا → إعادة الحزمة → Integrity Check.
